<!doctype html>
<html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.0.2/css/bootstrap.min.css"> <!-- load bootstrap css -->
  <link rel="stylesheet" href="//netdna.bootstrapcdn.com/font-awesome/4.0.3/css/font-awesome.min.css"> <!-- load fontawesome -->
  <link rel="stylesheet" href="stylesheets/formStyling.css">
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
  <script src="https://unpkg.com/xlsx/dist/xlsx.full.min.js"></script>
  <link rel='stylesheet prefetch' href='http://ajax.googleapis.com/ajax/libs/jqueryui/1.11.2/themes/smoothness/jquery-ui.css'>
</head>

<body style = "background-color: 	#DCDCDC">
<div style = "background-color: #FFFFF0; margin-top: 2%;" class = "container" id = "topDiv">
    <header class = "header" > <%= title[0].Title %> Form </header>
    <br>
    <br>
</div>
<br>
<div class= "container" style = "background-color: #FFFFF0;" id = "formDiv">

  <form  method = "POST" id = "form">
    <table class = "table" id ="myTable" style = "border: none;">
        <tr>
        <td type = "text" style = "text-align: center;" contenteditable = "false"><b>Attribute/Category<b></td>
          <% for (var i = 0; i < category.length; i++) { %>
            <td style = "text-align: center; font-weight: bold;" id = "<%= "0" + String(i+1) %>"><%=category[i].Description%></td>
              <% } %>
            </tr>
              <br>
      <% for (var i = 0; i < attribute.length; i++) { %>
        <tr>
          <td style = "text-align: center;" id = "<%=String(i+1)+ "0" %>"><%=attribute[i].Description%></td>
          <% for(var j = 0; j < category.length; j ++ ) { %>
            <td style = "text-align: center;"><input name = "<%= String(i+1) + String(j+1)%>" type = "text" style = "text-align:center;"></td>
          <% } %>
        </tr>
        <% } %>
    </table>
<input class="btn btn-warning" id = "submit" type = "submit" value = "Submit">
</form>
<br>
</div>
<div class= "container" style = "background-color: #FFFFF0;" id = "formDiv">
  <div id="wrapper" style = "text-align: center">
  <input type = "file" id = "input-excel"/>
  <br>
  <p>Please make sure you select the right excel file and make sure you name your excel sheet "sheet1"</p>
</div>
<table id = "newTable" class="table" >
<tr class = "hide"></tr>
</div>
<p class = "hide" id = "message1" style = "color: red"> Sorry, but the size of the excel file you uploaded does not match the size of the table above </p>
<button id = "editTable" class = "hide" style = "margin-top: 3%">Click here to edit/submit table </button>

</table>
<script>
var $TABLE = $('#newTable');
        $('#input-excel').change(function(e){
                var reader = new FileReader();
                reader.readAsArrayBuffer(e.target.files[0]);
                reader.onload = function(e) {
                        var data = new Uint8Array(reader.result);
                        var wb = XLSX.read(data,{type:'array'});
                        var htmlstr = XLSX.write(wb,{sheet:"Sheet1", type:'binary',bookType:'html'});

                      htmlstr =  htmlstr.replace(/<table>/, '<table id = "oldTable">');
                        console.log(htmlstr);
                        $('#wrapper')[0].innerHTML += htmlstr;
                        $('#editTable').removeClass('hide');
                }
        });
        $('#editTable').click(function()
        {
          var currentLetter = 'B';
          var currentNumber = 1;
          //check to see if user submitted right excel table/sheet
          var match = true;
          if((document.getElementById('oldTable').rows[0].cells.length == document.getElementById('myTable').rows[0].cells.length) && (document.getElementById('oldTable').rows.length == document.getElementById('myTable').rows.length))
          {
            // the size of the table matches, now we check the actual contents
            for( var i = 0; i < document.getElementById('oldTable').rows[0].cells.length-1; i ++ )
            {
              if(document.getElementById("0" + String(i+1)).innerHTML == document.getElementById("sjs-" + currentLetter + String(1)).innerHTML )
              {
                console.log("matched!");
                currentLetter = String.fromCharCode(currentLetter.charCodeAt(0) + 1);
              }
              else
              {
                console.log(document.getElementById("0" + String(i+1)).innerHTML);
                console.log(document.getElementById("sjs-E1").innerHTML);
                console.log("--------");
                  match = false;
              }
            }
            for( var j = 0; j < document.getElementById('oldTable').rows.length-1; j++)
            {
              currentLetter = "A";
              if(document.getElementById(String(j+1) + "0").innerHTML == document.getElementById("sjs-" + currentLetter + String(j+2)).innerHTML)
              {
                console.log("matched!!!")
              }
              else
              {
                  console.log(document.getElementById(String(j+1) + "0").innerHTML);
                  console.log(document.getElementById("sjs-" + currentLetter + String(j+2)).innerHTML);
                  console.log("-----");
                  match = false;
              }
            }
          }
          //size of tables don't match
          else
          {
            $('#message1').removeClass('hide');
          }
          if(match == true)
          {
            //bottom form starts at b2 top form starts at 11
          }
        });
        </script>
</body>
</html>
