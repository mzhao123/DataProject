var query = require('../models/query');

module.exports =
{

  makeTable: function(reqBody, reqUser, callback)
  {
    var row  = 1;
    var column = 1;
    var together = String(row) + String(column);
    while(reqBody.[together] != null)
    {
      while(reqBody.[together] != null)
      {
        
      }
    }
  }
}
